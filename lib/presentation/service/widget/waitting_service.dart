import 'package:flutter/widgets.dart';

class WaittingService extends StatelessWidget {
  const WaittingService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
